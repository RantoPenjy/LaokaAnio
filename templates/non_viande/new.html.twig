{% extends 'base.html.twig' %}

{% block title %}New NonViande{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center h-100 align-items-center">
        <div>
            <h1>Create new NonViande</h1>

            {{ include('non_viande/_form.html.twig') }}

            <a class="btn btn-outline-primary" href="{{ path('app_non_viande_index') }}">back to list</a>
        </div>
    </div>
{% endblock %}
