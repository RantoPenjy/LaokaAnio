<form method="post" action="{{ path('app_non_viande_delete', {'id': non_viande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ non_viande.id) }}">
    <button class="btn">Delete</button>
</form>
