{% extends 'base.html.twig' %}

{% block title %}Viande{% endblock %}

{% block body %}
    <h1>Viande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viande.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ viande.name }}</td>
            </tr>
            <tr>
                <th>Min_price_per_person</th>
                <td>{{ viande.minPricePerPerson }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_viande_index') }}">back to list</a>

    <a href="{{ path('app_viande_edit', {'id': viande.id}) }}">edit</a>

    {{ include('viande/_delete_form.html.twig') }}
{% endblock %}
